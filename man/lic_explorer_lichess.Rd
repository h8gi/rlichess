% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explorer.R
\name{lic_explorer_lichess}
\alias{lic_explorer_lichess}
\alias{lic_opening_explorer}
\title{Query Lichess Opening Explorer Database}
\usage{
lic_explorer_lichess(
  fen = NULL,
  play = NULL,
  variant = "standard",
  ratings = c(2000, 2200, 2500),
  speeds = c("blitz", "rapid"),
  token = lic_token()
)

lic_opening_explorer(
  fen = NULL,
  play = NULL,
  variant = "standard",
  ratings = c(2000, 2200, 2500),
  speeds = c("blitz", "rapid"),
  token = lic_token()
)
}
\arguments{
\item{fen}{FEN position string. Default is the starting chess position.}

\item{play}{Comma-separated or vector of move sequence in UCI or SAN (e.g. \code{"e2e4,e7e5"} or \code{c("e4", "e5")}).}

\item{variant}{Chess variant (\code{"standard"}, \code{"chess960"}, etc.). Default is \code{"standard"}.}

\item{ratings}{Integer vector of rating brackets (e.g. \code{c(1600, 1800, 2000, 2200, 2500)}).}

\item{speeds}{Character vector of game speeds (e.g. \code{c("bullet", "blitz", "rapid", "classical")}).}

\item{token}{API access token. By default, retrieved via \code{\link[=lic_token]{lic_token()}}.}
}
\value{
A list containing aggregate stats (\code{white}, \code{draws}, \code{black}) and a tidy \link[tibble:tibble]{tibble::tibble} of candidate \code{moves}.
}
\description{
Retrieves opening statistics (win rates, popular candidate moves) from the Lichess game database.
}
\examples{
\dontrun{
exp <- lic_explorer_lichess(play = "e4,c5")
exp$moves
}
}
