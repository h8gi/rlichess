% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/games.R
\name{lic_games_user}
\alias{lic_games_user}
\alias{lic_get_games}
\title{Download Games of a Lichess User}
\usage{
lic_games_user(
  username,
  perf_type = NULL,
  since = NULL,
  until = NULL,
  max = NULL,
  vs = NULL,
  rated = NULL,
  color = NULL,
  analysed = NULL,
  opening = TRUE,
  moves = TRUE,
  clocks = FALSE,
  evals = FALSE,
  token = lic_token()
)

lic_get_games(
  username,
  perf_type = NULL,
  since = NULL,
  until = NULL,
  max = NULL,
  vs = NULL,
  rated = NULL,
  color = NULL,
  analysed = NULL,
  opening = TRUE,
  moves = TRUE,
  clocks = FALSE,
  evals = FALSE,
  token = lic_token()
)
}
\arguments{
\item{username}{Lichess username.}

\item{perf_type}{Optional game type filter (e.g. \code{"bullet"}, \code{"blitz"}, \code{"rapid"}, \code{"classical"}).}

\item{since}{Download games played since this timestamp or Date (e.g. \code{"2025-01-01"} or \code{as.Date(...)}).}

\item{until}{Download games played until this timestamp or Date.}

\item{max}{Maximum number of games to download. Default is \code{NULL} (all games).}

\item{vs}{Filter for games played against a specific opponent username.}

\item{rated}{Logical. Whether to fetch only rated games. Default is \code{NULL}.}

\item{color}{Filter for player's color: \code{"white"} or \code{"black"}.}

\item{analysed}{Logical. Whether to fetch only games with computer analysis.}

\item{opening}{Logical. Whether to include opening name and ECO code. Default is \code{TRUE}.}

\item{moves}{Logical. Whether to include PGN moves string. Default is \code{TRUE}.}

\item{clocks}{Logical. Whether to include clock times for each move. Default is \code{FALSE}.}

\item{evals}{Logical. Whether to include Stockfish evaluations. Default is \code{FALSE}.}

\item{token}{API access token. By default, retrieved via \code{\link[=lic_token]{lic_token()}}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} containing raw game records.
}
\description{
Fetches game data for a given user from the Lichess API in NDJSON format.
}
\details{
\strong{Lichess API Endpoint:} \code{GET /api/games/user/{username}}

\strong{Official Documentation:} \url{https://lichess.org/api#tag/Games/operation/apiGamesUser}

Rate limits: Anonymous requests are throttled at 20 games/sec. Authenticated
OAuth requests receive 30 games/sec (or 60 games/sec when fetching your own games).
}
\examples{
\dontshow{if (nzchar(Sys.getenv("LICHESS_API_TOKEN"))) withAutoprint(\{ # examplesIf}
games <- lic_games_user("h8gi", perf_type = "bullet", max = 5)
\dontshow{\}) # examplesIf}
}
