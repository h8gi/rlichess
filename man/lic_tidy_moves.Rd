% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/games.R
\name{lic_tidy_moves}
\alias{lic_tidy_moves}
\title{Tidy Game Moves, Clocks, and Evaluations}
\usage{
lic_tidy_moves(data)
}
\arguments{
\item{data}{A game tibble containing an \code{id} and a \code{moves} column.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with columns:
\item{game_id}{Lichess game identifier}
\item{ply}{Half-move index (1, 2, 3, ...)}
\item{move_number}{Full-move number (1, 1, 2, 2, ...)}
\item{color}{Color playing the move (\code{"white"} or \code{"black"})}
\item{san}{Standard Algebraic Notation of the move (e.g. \code{"e4"}, \code{"Nf3"})}
\item{clock}{Remaining clock time in seconds (if available)}
\item{eval}{Stockfish evaluation in centipawns or mate (if available)}
}
\description{
Expands game move sequences, clock times, and Stockfish evaluations into a
long-format tidy tibble where each row corresponds to one half-move (ply).
}
\details{
This is an \strong{offline data transformation} function (no API network request).
It unnests the move string (\code{moves}), clock array (\code{clocks}), and evaluation array (\code{evals})
from game records into a tidy ply-by-ply dataset.
}
\examples{
sample_game <- tibble::tibble(
  id = "demo123",
  moves = "e4 e5 Nf3 Nc6 Bb5"
)
lic_tidy_moves(sample_game)
}
